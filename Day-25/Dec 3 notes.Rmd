Dec 3, 2013 Day-25 notes
========================================================

Vote Problem:

Conditional
  If and else
  is.majoirty <- function()
  return(name, number of votes, and total votes)
    If no winner, return(NULL)

is.majority(votes)

what.percentage(votes)
return(name, # of votes, percent of votes)
Each candidate appears once

table

ddply(votes, ,summarize,)





